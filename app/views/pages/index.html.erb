<h1>Listing pages</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.name %></td>
    <td><%= page.company.name %></td>
    <td><%= link_to 'Show', [page.company, page] %></td>
    <td><%= link_to 'Edit', [:edit, page.company, page] %></td>
    <td><%= link_to 'Destroy', [page.company, page], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_company_page_path(Company.first) %>
